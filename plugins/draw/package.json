{
  "name": "@crabcode/draw",
  "version": "0.2.0",
  "description": "Collaborative Excalidraw sessions for crabcode",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "crab-draw": "./bin/crab-draw.js"
  },
  "scripts": {
    "build": "tsc && cd ui && npm install && npm run build",
    "build:server": "tsc",
    "build:ui": "cd ui && npm install && npm run build",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "nanoid": "^5.0.0",
    "open": "^10.0.0",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
